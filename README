linux_build:  contains files for running/building on linux
src:  contains the cpp files
include: contains the header files
data: contains the uai and evidence files



linux_build:
	makefile: makes the original uai solver and the new interface   
        	  usage: make uai_1
		  	 make bp_interface
			       
	runInterface:  runs the test interface with the test.uai file and task PR
	runScript:     makes and runs both uai14 solver and BPinterface solver for comparison


src:
	BPinterface.cpp: the interface file for APRIL
	test_interface.cpp: a testing file for BpInterface
	uai14.cpp: the original uai solving file